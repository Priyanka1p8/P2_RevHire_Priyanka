package com.rev.app;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;

/**
 * Smoke test — verifies the test harness itself boots without errors.
 * Uses JUnit 4 (compatible with pom.xml dependency on junit:junit:4.13.2).
 */
@RunWith(MockitoJUnitRunner.class)
public class RevhireP2ApplicationTests {

    @Test
    public void contextLoads() {
        // Placeholder smoke test.
        // Full service/repository tests are in com.rev.app.service package.
    }
}
